<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Isha Farm Stay – Luxury Experience</title>
  
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet" />

  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: #0c0c0c;
      color: #fff;
      overflow-x: hidden;
    }

    /* Luxury Gold Text */
    .gold {
      background: linear-gradient(45deg, #d4af37, #fff1a1, #b8860b);
      -webkit-background-clip: text;
      color: transparent;
    }

    /* Floating Navbar */
    nav {
      position: fixed;
      top: 0;
      width: 100%;
      display: flex;
      justify-content: center;
      padding: 20px;
      background: rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(10px);
      z-index: 999;
      transition: 0.3s;
    }

    nav.scrolled {
      padding: 10px;
      background: rgba(0, 0, 0, 0.7);
    }

    nav a {
      margin: 0 20px;
      color: #fff;
      text-decoration: none;
      font-size: 1.1rem;
      letter-spacing: 1px;
    }

    /* Hero Section with Particles */
    .hero {
      height: 90vh;
      background: url('https://images.unsplash.com/photo-1501785888041-af3ef285b470?auto=format&fit=crop&w=1400&q=80') center/cover no-repeat;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .hero::after {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0,0,0,0.5);
    }

    .hero h1 {
      font-family: 'Playfair Display';
      font-size: 4rem;
      z-index: 2;
      margin: 0;
    }

    /* Floating Gold Particles */
    @keyframes particles {
      0% { transform: translateY(0) translateX(0); opacity: 0.4; }
      50% { opacity: 1; }
      100% { transform: translateY(-200px) translateX(50px); opacity: 0; }
    }

    .particle {
      position: absolute;
      width: 6px;
      height: 6px;
      background: gold;
      border-radius: 50%;
      animation: particles 6s linear infinite;
    }

    /* Premium Section */
    .section {
      padding: 60px 10%;
    }

    .section-title {
      font-size: 2.4rem;
      text-align: center;
      margin-bottom: 40px;
      font-family: 'Playfair Display';
    }

    /* Glass Card Gallery */
    .gallery {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 25px;
    }

    .gallery img {
      width: 100%;
      height: 250px;
      object-fit: cover;
      border-radius: 16px;
      border: 1px solid rgba(255,255,255,0.2);
      box-shadow: 0 8px 25px rgba(0,0,0,0.5);
      transition: .4s;
      backdrop-filter: blur(10px);
    }

    .gallery img:hover {
      transform: scale(1.05) rotate(1deg);
      box-shadow: 0 12px 35px rgba(255,215,0,0.5);
    }

    /* Luxury Counters */
    .counters {
      display: flex;
      justify-content: space-around;
      margin: 60px 0;
      text-align: center;
    }

    .counter-box {
      font-size: 2.5rem;
      font-family: 'Playfair Display';
    }

    /* Footer */
    footer {
      text-align: center;
      padding: 40px;
      background: #000;
      font-size: 1.1rem;
      letter-spacing: 1px;
    }
  </style>

  <script>
    // Navbar scroll shrink
    window.addEventListener('scroll', () => {
      document.querySelector('nav').classList.toggle('scrolled', window.scrollY > 50);
    });

    // Counter animation
    const counters = document.querySelectorAll('.counter');
    const speed = 200;

    counters.forEach(counter => {
      const animate = () => {
        const value = +counter.getAttribute('data-target');
        const data = +counter.innerText;
        const time = value / speed;

        if (data < value) {
          counter.innerText = Math.ceil(data + time);
          setTimeout(animate, 10);
        } else {
          counter.innerText = value;
        }
      };

      animate();
    });
  </script>
</head>
<body>

<nav>
  <a href="#home">Home</a>
  <a href="#gallery">Gallery</a>
  <a href="#luxury">Luxury</a>
  <a href="#contact">Contact</a>
</nav>

<div class="hero" id="home">
  <h1 class="gold">Isha Farm Stay</h1>
</div>

<div class="section" id="luxury">
  <h2 class="section-title gold">Luxury Counters</h2>
  <div class="counters">
    <div class="counter-box"><span class="counter" data-target="250">0</span><br> Guests</div>
    <div class="counter-box"><span class="counter" data-target="25">0</span><br> Acres</div>
    <div class="counter-box"><span class="counter" data-target="12">0</span><br> Rooms</div>
  </div>
</div>

<div class="section" id="gallery">
  <h2 class="section-title gold">Exclusive Gallery</h2>

  <div class="gallery">
    <img src="sandbox:/mnt/data/WhatsApp Image 2025-12-01 at 3.36.25 PM.jpeg" />
    <img src="sandbox:/mnt/data/WhatsApp Image 2025-12-01 at 3.36.24 PM (1).jpeg" />
    <img src="sandbox:/mnt/data/WhatsApp Image 2025-12-01 at 3.36.24 PM.jpeg" />
    <img src="sandbox:/mnt/data/WhatsApp Image 2025-12-01 at 3.36.23 PM (1).jpeg" />
    <img src="sandbox:/mnt/data/WhatsApp Image 2025-12-01 at 3.36.23 PM.jpeg" />
  </div>
</div>

<footer id="contact">
  ©️ 2025 Isha Farm Stay – Luxury Redefined
</footer>

  
<!-- Luxury Level 2 Additions -->

<style>
  /* PARALLAX SECTION */
  .parallax {
    height: 70vh;
    background-image: url('https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?auto=format&fit=crop&w=1500&q=80');
    background-attachment: fixed;
    background-position: center;
    background-size: cover;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 3rem;
    font-family: 'Playfair Display';
    color: gold;
    text-shadow: 0 0 20px #000;
  }

  /* FLOATING BOOKING BUTTON */
  .book-btn {
    position: fixed;
    right: 25px;
    bottom: 25px;
    padding: 18px 35px;
    font-size: 1.3rem;
    background: linear-gradient(45deg, #ffdd55, #b8860b);
    border-radius: 50px;
    color: #000;
    font-weight: bold;
    box-shadow: 0 5px 20px rgba(255,215,0,0.6);
    cursor: pointer;
    animation: pulse 2s infinite;
    z-index: 9999;
  }

  @keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.08); }
    100% { transform: scale(1); }
  }

  /* 3D PARALLAX IMAGES */
  .parallax-card {
    perspective: 1000px;
  }

  .parallax-card img {
    width: 100%;
    border-radius: 16px;
    transition: transform .2s;
  }

  .parallax-card:hover img {
    transform: rotateY(12deg) rotateX(8deg) scale(1.05);
    box-shadow: 0 20px 40px rgba(255,215,0,0.4);
  }

  /* GOLD BORDER ANIMATION */
  .gold-border {
    position: relative;
    padding: 25px;
    border-radius: 16px;
    color: #fff;
    margin-bottom: 30px;
  }

  .gold-border::before {
    content: "";
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    background: linear-gradient(45deg, gold, transparent, gold);
    border-radius: 16px;
    z-index: -1;
    filter: blur(4px);
    animation: borderGlow 3s linear infinite;
  }

  @keyframes borderGlow {
    0% { filter: blur(4px) brightness(1); }
    50% { filter: blur(6px) brightness(1.8); }
    100% { filter: blur(4px) brightness(1); }
  }

  /* FULLSCREEN IMAGE VIEWER */
  .viewer-bg {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    background: rgba(0,0,0,0.9);
    backdrop-filter: blur(6px);
    justify-content: center;
    align-items: center;
    z-index: 99999;
  }

  .viewer-bg img {
    max-width: 80%;
    max-height: 80%;
    border-radius: 10px;
    box-shadow: 0 0 25px rgba(255,215,0,0.6);
  }
</style>

<div class="parallax">Luxury • Nature • Peace</div>

<div class="section">
  <h2 class="section-title gold">3D Premium Parallax Images</h2>
  <div class="gallery">
    <div class="parallax-card"><img src="sandbox:/mnt/data/WhatsApp Image 2025-12-01 at 3.36.25 PM.jpeg"></div>
    <div class="parallax-card"><img src="sandbox:/mnt/data/WhatsApp Image 2025-12-01 at 3.36.24 PM.jpeg"></div>
    <div class="parallax-card"><img src="sandbox:/mnt/data/WhatsApp Image 2025-12-01 at 3.36.23 PM (1).jpeg"></div>
  </div>
</div>

<div class="section gold-border">
  <h2 class="section-title gold">Golden Highlight Section</h2>
  <p style="font-size:1.2rem; line-height:1.7;">
    Experience unmatched luxury with our gold-accented stay experience designed to create unforgettable memories.
  </p>
</div>

<!-- Fullscreen Image Viewer -->
<div class="viewer-bg" id="viewer">
  <img id="viewer-img">
</div>

<!-- Floating Booking Button -->
<div class="book-btn">Book Now</div>

<script>
  // FULLSCREEN VIEWER
  const images = document.querySelectorAll('.gallery img');
  const viewer = document.getElementById('viewer');
  const viewerImg = document.getElementById('viewer-img');

  images.forEach(img => {
    img.addEventListener('click', () => {
      viewer.style.display = 'flex';
      viewerImg.src = img.src;
    });
  });

  viewer.addEventListener('click', () => viewer.style.display = 'none');
</script>

</body>
</html>
